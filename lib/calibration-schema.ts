import { z } from "zod";
export const calibrationApplicationSchema=z.object({fullName:z.string().min(2).max(100),workEmail:z.string().email(),jobTitle:z.string().min(2).max(100),agencyName:z.string().min(2).max(150),agencyWebsite:z.string().url(),country:z.string().min(2),primaryMarket:z.string().min(2),specializations:z.array(z.string()).min(1).max(12),typicalEmployerProfile:z.string().min(10).max(1500),credibleRoles:z.string().min(5).max(1500),acquisitionChannels:z.array(z.string()).min(1).max(10),recentAcquisitionSuccess:z.string().max(1500),paidPilotReadiness:z.enum(["Yes","Possibly, depending on results","Not currently"]),weeklyFeedbackAvailability:z.enum(["Yes","Possibly","No"]),contactConsent:z.literal(true),privacyConsent:z.literal(true),website:z.string().max(0).optional(),utm:z.record(z.string(),z.string()).optional()});
