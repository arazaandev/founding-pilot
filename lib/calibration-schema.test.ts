import{describe,expect,it}from"vitest";import{calibrationApplicationSchema}from"./calibration-schema";
const valid={fullName:"Daffa Test",workEmail:"dtest@example.org",jobTitle:"Founder",agencyName:"Example Recruitment",agencyWebsite:"https://example.org",country:"Indonesia",primaryMarket:"Indonesia",specializations:["Technology"],typicalEmployerProfile:"Indonesian financial-services companies.",credibleRoles:"Senior data roles",acquisitionChannels:["Referrals and existing relationships"],recentAcquisitionSuccess:"A referral led to an introduction.",paidPilotReadiness:"Yes",weeklyFeedbackAvailability:"Yes",contactConsent:true,privacyConsent:true,website:"",utm:{utm_source:"linkedin"}};
describe("calibration application schema",()=>{it("accepts a complete lightweight application",()=>expect(calibrationApplicationSchema.safeParse(valid).success).toBe(true));it("rejects a completed spam honeypot",()=>expect(calibrationApplicationSchema.safeParse({...valid,website:"spam"}).success).toBe(false));it("requires explicit privacy consent",()=>expect(calibrationApplicationSchema.safeParse({...valid,privacyConsent:false}).success).toBe(false));it("restricts commercial readiness to the published choices",()=>expect(calibrationApplicationSchema.safeParse({...valid,paidPilotReadiness:"Maybe later"}).success).toBe(false))});
