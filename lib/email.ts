type Input={fullName:string;workEmail:string;agencyName:string;jobTitle:string;specializations:string[];paidPilotReadiness:string;weeklyFeedbackAvailability:string};
const esc=(value:string)=>value.replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[char]!));
export async function sendApplicationEmails(input:Input){
  const apiKey=process.env.RESEND_API_KEY,from=process.env.EMAIL_FROM,admin=process.env.ADMIN_EMAIL;
  if(!apiKey||!from){console.info("Email adapter: console",{applicant:input.workEmail,admin});return}
  const messages=[{from,to:[input.workEmail],subject:"We received your Lancara calibration application",html:`<p>Hi ${esc(input.fullName)},</p><p>Thank you for applying for a free Lancara Opportunity Calibration. We will manually review whether the Hiring Demand Playbook can be configured around your agency’s market.</p><p>Selected agencies will be invited to a 30-minute configuration session. Applying does not guarantee selection.</p>`},...(admin?[{from,to:[admin],subject:`New Lancara calibration application: ${input.agencyName}`,html:`<p><strong>${esc(input.fullName)}</strong>, ${esc(input.jobTitle)} at ${esc(input.agencyName)}</p><p>Specializations: ${esc(input.specializations.join(", "))}</p><p>Rp4m readiness: ${esc(input.paidPilotReadiness)}</p><p>Weekly feedback: ${esc(input.weeklyFeedbackAvailability)}</p><p>Qualification is manual; review the application in the admin workspace.</p>`}]:[])];
  for(const message of messages){const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify(message)});if(!response.ok)throw new Error(`Email delivery failed (${response.status})`)}
}
