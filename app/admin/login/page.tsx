"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function Login(){const[p,setP]=useState(""),[e,setE]=useState(""),r=useRouter();async function go(){const x=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:p})});if(x.ok)r.push("/admin");else setE("Incorrect password")};return <main><section><div className="container form"><h1 style={{fontSize:48}}>Admin access</h1><div className="field"><label>Password</label><input type="password" value={p} onChange={x=>setP(x.target.value)}/></div>{e&&<p role="alert">{e}</p>}<button className="button" onClick={go}>Sign in</button></div></section></main>}
